<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SEH.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SEH"
    xmlns:views="using:SEH.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="So Easy 口琴">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base" />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <!--标题栏-->
            <RowDefinition Height="Auto"/>
            <!--内容区-->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="So Easy 口琴" 
                  IsBackButtonVisible="False" 
                  IsPaneToggleButtonVisible="False">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Images/口琴48.png" />
            </TitleBar.IconSource>
            <TitleBar.Content>
                <CommandBar Background="Transparent" IsOpen="False" DefaultLabelPosition="Right">
                    <AppBarButton Icon="Add" Label="新增类别"/>
                    <AppBarButton Icon="Edit" Label="修改类别"/>
                    <AppBarButton Icon="Delete" Label="删除类别"/>
                    <AppBarSeparator/>
                    <AppBarButton Icon="Add" Label="新增简谱"/>
                    <AppBarButton Icon="Edit" Label="修改简谱"/>
                    <AppBarButton Icon="Delete" Label="删除简谱"/>
                    <AppBarButton Icon="MoveToFolder" Label="移动简谱"/>
                </CommandBar>
            </TitleBar.Content>
        </TitleBar>

        <!-- App content -->
        <Frame x:Name="rootFrame" Grid.Row="1" SourcePageType="views:AllNotesPage"/>

        <!--<StackPanel HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Width="250">

            -->
        <!--显示当前计数的文本，绑定到 ViewModel 中的 Count 属性-->
        <!--
            <TextBlock Text="{x:Bind ViewModel.Count, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                       FontSize="48" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>


            -->
        <!--三个按钮的 Command 属性分别绑定到自动生成的命令-->
        <!--
            <Button Content="增加 (+)" 
                    Command="{x:Bind ViewModel.IncrementCommand}"
                    Height="40" 
                    Margin="0,5"
                    Background="LightGreen"/>

            <Button Content="减少 (-)" 
                    Command="{x:Bind ViewModel.DecrementCommand}"
                    Height="40" 
                    Margin="0,5"
                    Background="LightCoral"/>

            <Button Content="重置" 
                    Command="{x:Bind ViewModel.ResetCommand}"
                    Height="40" 
                    Margin="0,5"
                    Background="LightGray"/>
        </StackPanel>-->

    </Grid>
</Window>
