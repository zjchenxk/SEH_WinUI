<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SEH.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SEH"
    xmlns:views="using:SEH.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="So Easy 口琴">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base" />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <!--标题栏-->
            <RowDefinition Height="Auto"/>
            <!--内容区-->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <!--自定义标题栏样式-->
            <Style x:Key="MyTitleBarStyle" TargetType="TitleBar">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TitleBar">
                            <Grid Background="{TemplateBinding Background}" Height="56">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- 图标和标题区域 -->
                                <StackPanel Orientation="Horizontal" Grid.Column="0" Spacing="12" Padding="20,0,0,0">
                                    <!-- 图标 -->
                                    <ContentControl Content="{TemplateBinding IconSource}" Width="48" Height="48" VerticalAlignment="Center">
                                        <ContentControl.ContentTemplate>
                                            <DataTemplate>
                                                <Image Source="{Binding ImageSource}" Width="48" Height="48" Stretch="Uniform"/>
                                            </DataTemplate>
                                        </ContentControl.ContentTemplate>
                                    </ContentControl>
                                    <!-- 标题 -->
                                    <TextBlock Text="{TemplateBinding Title}"
                                               FontSize="32"
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"
                                               Foreground="{TemplateBinding Foreground}"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>

                                <!-- 自定义内容区域 -->
                                <ContentPresenter Content="{TemplateBinding Content}"
                                                Grid.Column="1"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Margin="20,0"/>

                                <!-- 底部边框 -->
                                <Border VerticalAlignment="Bottom"
                                        Grid.ColumnSpan="2"
                                        Height="1"
                                        BorderBrush="{ThemeResource TextControlForeground}"
                                        BorderThickness="0,0,0,1"/>

                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- DataTemplate for folders -->
            <DataTemplate x:Key="FolderTemplate" x:DataType="local:ExplorerItem">
                <TreeViewItem AutomationProperties.Name="{x:Bind Name}"
                ItemsSource="{x:Bind Children}" IsExpanded="True">
                    <StackPanel Orientation="Horizontal">
                        <Image Width="32" Source="/Assets/Images/关闭文件夹.png"/>
                        <TextBlock Margin="0,0,10,0"/>
                        <TextBlock Text="{x:Bind Name}" />
                    </StackPanel>
                </TreeViewItem>
            </DataTemplate>

            <!-- DataTemplate for files -->
            <DataTemplate x:Key="FileTemplate" x:DataType="local:ExplorerItem">
                <TreeViewItem AutomationProperties.Name="{x:Bind Name}">
                    <StackPanel Orientation="Horizontal">
                        <Image Width="32" Source="/Assets/Images/查看乐谱.png"/>
                        <TextBlock Margin="0,0,10,0"/>
                        <TextBlock Text="{x:Bind Name}"/>
                    </StackPanel>
                </TreeViewItem>
            </DataTemplate>

            <!-- Template selector for ExplorerItem types -->
            <local:ExplorerItemTemplateSelector x:Key="ExplorerItemTemplateSelector" 
                                                FolderTemplate="{StaticResource FolderTemplate}" 
                                                FileTemplate="{StaticResource FileTemplate}" />
        </Grid.Resources>

        <!--标题栏-->
        <TitleBar x:Name="AppTitleBar" Title="So Easy 口琴" 
                  IsBackButtonVisible="False" 
                  IsPaneToggleButtonVisible="False"
                  Style="{StaticResource MyTitleBarStyle}">

            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Images/口琴48.png" />
            </TitleBar.IconSource>

            <TitleBar.Content>
                <CommandBar Background="Transparent" IsOpen="False" DefaultLabelPosition="Right">
                    <AppBarButton Label="新增类别" Style="{StaticResource MyAppBarButtonStyle}">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="/Assets/Images/添加.png" ShowAsMonochrome="False"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Label="修改类别" Style="{StaticResource MyAppBarButtonStyle}">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="/Assets/Images/修改.png" ShowAsMonochrome="False"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Label="删除类别" Style="{StaticResource MyAppBarButtonStyle}">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="/Assets/Images/删除.png" ShowAsMonochrome="False"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarSeparator/>
                    <AppBarButton Label="新增简谱" Style="{StaticResource MyAppBarButtonStyle}">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="/Assets/Images/添加.png" ShowAsMonochrome="False"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Label="修改简谱" Style="{StaticResource MyAppBarButtonStyle}">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="/Assets/Images/修改.png" ShowAsMonochrome="False"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Label="删除简谱" Style="{StaticResource MyAppBarButtonStyle}">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="/Assets/Images/删除.png" ShowAsMonochrome="False"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Label="移动简谱" Style="{StaticResource MyAppBarButtonStyle}">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="/Assets/Images/移动.png" ShowAsMonochrome="False"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                </CommandBar>
            </TitleBar.Content>
        </TitleBar>

        <SplitView Grid.Row="1" 
                   IsPaneOpen="True" 
                   DisplayMode="Inline" 
                   OpenPaneLength="300">
            <SplitView.Pane>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!--树形列表标题-->
                    <TextBlock x:Name="PaneHeader" 
                               Margin="20,10" 
                               Style="{StaticResource SubtitleTextBlockStyle}" 
                               Text="我的简谱"/>

                    <!--简谱树形列表-->
                    <TreeView x:Name="ScoreTreeView"
                              Grid.Row="1"
                              ItemsSource="{x:Bind DataSource}"
                              ItemTemplateSelector="{StaticResource ExplorerItemTemplateSelector}"
                              VerticalAlignment="Stretch"
                              HorizontalAlignment="Stretch"/>

                </Grid>
            </SplitView.Pane>

            <!--工作区-->
            <Frame x:Name="rootFrame" SourcePageType="views:AllNotesPage"/>

        </SplitView>

        <!--<StackPanel HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Width="250">

            -->
        <!--显示当前计数的文本，绑定到 ViewModel 中的 Count 属性-->
        <!--
            <TextBlock Text="{x:Bind ViewModel.Count, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                       FontSize="48" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>


            -->
        <!--三个按钮的 Command 属性分别绑定到自动生成的命令-->
        <!--
            <Button Content="增加 (+)" 
                    Command="{x:Bind ViewModel.IncrementCommand}"
                    Height="40" 
                    Margin="0,5"
                    Background="LightGreen"/>

            <Button Content="减少 (-)" 
                    Command="{x:Bind ViewModel.DecrementCommand}"
                    Height="40" 
                    Margin="0,5"
                    Background="LightCoral"/>

            <Button Content="重置" 
                    Command="{x:Bind ViewModel.ResetCommand}"
                    Height="40" 
                    Margin="0,5"
                    Background="LightGray"/>
        </StackPanel>-->

    </Grid>
</Window>
