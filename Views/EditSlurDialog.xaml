<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="SEH.Views.EditSlurDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SEH.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:SEH.ViewModels" 
    d:DataContext="{d:DesignInstance Type=viewmodels:EditSlurViewModel}"
    mc:Ignorable="d"
    Title="新增连音线"
    PrimaryButtonText="确定"
    SecondaryButtonText="取消"
    DefaultButton="Primary">

    <StackPanel Orientation="Vertical" Width="300">
        <!--连音线名称-->
        <StackPanel HorizontalAlignment="Stretch">
            <TextBox x:Name="SlurName"
                     Text="{x:Bind ViewModel.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     PlaceholderText="输入连音线名称..."
                     Header="连音线名称："
                     MaxLength="30"
                     HorizontalAlignment="Stretch"/>

            <!--红字错误提示，绑定到 ViewModel 的 NameError 属性-->
            <!--如果没有错误，该属性为 null，TextBlock 会自动隐藏-->
            <TextBlock Text="{x:Bind ViewModel.NameError, Mode=OneWay}" 
                       Foreground="{StaticResource SystemErrorTextColor}" 
                       FontSize="{StaticResource ToolTipContentThemeFontSize}" 
                       Margin="0,4,0,0"/>
        </StackPanel>
    </StackPanel>
</ContentDialog>
